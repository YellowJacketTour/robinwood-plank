import {chromium} from 'playwright';import assert from 'node:assert/strict';import {randomUUID} from 'node:crypto';
const browser=await chromium.launch();const base='http://localhost:3017';
try{const page=await browser.newPage();const response=await page.request.post(base+'/api/charmville/local-playtest',{headers:{Origin:base}});assert.equal(response.status(),200);const user=await response.json();const claim=await page.request.post(`${base}/api/charmville/${user.handle}`,{headers:{Origin:base,authorization:`Bearer ${user.token}`},data:{action:'claim',requestId:randomUUID()}});assert.equal(claim.status(),200);
await page.addInitScript(({wallet,token})=>{for(const type of ['plank:wallet-state','plank:wallet-response'])window.addEventListener(type,e=>{const state=type==='plank:wallet-state'?e.detail:e.detail?.result?.state;if(state&&state.address!==wallet)e.stopImmediatePropagation();},true);localStorage.setItem('plankspace-last-verified-wallet',wallet);localStorage.setItem('plankspace-session:'+wallet,token);window.addEventListener('plank:wallet-request',e=>{if(e.detail.method==='getState')window.dispatchEvent(new CustomEvent('plank:wallet-response',{detail:{requestId:e.detail.requestId,result:{state:{address:wallet,status:'connected',isConnected:true,chainId:null}}}}));});},user);
const headers={Origin:base,authorization:`Bearer ${user.token}`};const presence=await(await page.request.get(base+'/api/charmville/world/presence',{headers})).json();assert.equal((await page.request.post(base+'/api/charmville/world/presence',{headers,data:{destination:'home',handle:user.handle,revision:presence.revision}})).status(),200);assert.equal((await page.request.get(base+'/api/charmville/world/actor',{headers})).status(),200);assert.equal((await page.request.post(base+'/api/charmville/companions',{headers,data:{speciesId:277}})).status(),200);await page.request.get(base+'/api/charmville/creature-roster',{headers});assert.equal((await page.request.post(base+'/api/charmville/local-playtest/party',{headers,data:{action:'provision'}})).status(),200);await page.setViewportSize({width:390,height:844});await page.goto(base+'/charmville/world?panel=play');const encounter=page.getByRole('region',{name:'Nearby encounter',exact:true});await encounter.getByRole('button',{name:'Meet Poochyena',exact:true}).waitFor({timeout:60000});await encounter.getByRole('button',{name:'Meet Poochyena',exact:true}).focus();await page.keyboard.press('Enter');await encounter.getByRole('button',{name:'Inspect',exact:true}).click();await encounter.getByText(/Inspecting this creature/).waitFor();const inspected=await(await page.request.get(base+'/api/charmville/world/encounter',{headers})).json();assert.equal(inspected.encounter.mode,'turn');assert.equal(inspected.encounter.controllerId,inspected.profileId);const beforeBattle=await(await page.request.get(base+'/api/charmville/world/battle',{headers})).json();let firstRequest=null,committed=null,posts=0;await page.route('**/api/charmville/world/battle',async route=>{if(route.request().method()!=='POST'){await route.continue();return;}posts++;const command=route.request().postDataJSON();if(posts===1){firstRequest=command;const response=await route.fetch();assert.equal(response.status(),200);committed=await response.json();await route.abort();return;}assert.deepEqual(command,firstRequest);await route.continue();});const battle=page.getByRole('region',{name:'Encounter battle',exact:true});await battle.getByRole('button',{name:/Pound/i}).click();await battle.getByRole('button',{name:'Retry battle turn',exact:true}).click();await battle.getByText(/Turn 1 saved/).waitFor();const afterBattle=await(await page.request.get(base+'/api/charmville/world/battle',{headers})).json();assert.equal(afterBattle.turn,1);assert.equal(afterBattle.partner.move.pp,beforeBattle.eligible[0].move.pp-1);assert.equal(afterBattle.partner.hp,committed.partner.hp);assert.equal(afterBattle.wild.hp,committed.wild.hp);assert.equal(posts,2);await battle.screenshot({path:'work/battle-mobile.png'});await page.unroute('**/api/charmville/world/battle');const pikachu=afterBattle.eligible.find(x=>x.speciesId===25);await battle.getByRole('combobox',{name:'Switch companion',exact:true}).selectOption(pikachu.id);await battle.getByRole('button',{name:'Switch now',exact:true}).click();await battle.getByText(/Turn 2 saved/).waitFor();const switched=await(await page.request.get(base+'/api/charmville/world/battle',{headers})).json();assert.equal(switched.partner.id,pikachu.id);assert.equal(switched.turn,2);assert.equal(switched.partner.move.pp,pikachu.move.pp);assert.equal(switched.wild.hp,afterBattle.wild.hp);await encounter.getByRole('img',{name:'Pikachu source portrait',exact:true}).waitFor();console.log('Six-member actual roster supports selected Pikachu battle switch; switch consumes turn, no own PP debit or wild damage.');
}finally{await browser.close();}
