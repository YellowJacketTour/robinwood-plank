import{JsonRpcProvider,Contract,hexlify,randomBytes}from'ethers';import{readFileSync}from'node:fs';
const p=new JsonRpcProvider('http://127.0.0.1:8545');if((await p.getNetwork()).chainId!==31337n)throw Error('Local only');const signer=await p.getSigner(0);const c=new Contract('0xA56F946D6398Dd7d9D4D9B337Cf9E0F68982ca5B',JSON.parse(readFileSync('public/arcade/abi/PlankCrash.json')),signer);const r=await c.currentRound();const block=await p.getBlock('latest');
if(r.phase===1n&&BigInt(block.timestamp)>=r.revealNotBefore){const beacon=new Contract(await c.beacon(),['function randomnessOrZero(uint64) view returns(bytes32)','function setRandomness(uint64,bytes32)'],signer);if(BigInt(await beacon.randomnessOrZero(r.targetDrandRound))===0n)await(await beacon.setRandomness(r.targetDrandRound,hexlify(randomBytes(32)))).wait();await(await c.settleRound()).wait();console.log('Recovered previous local round settlement');}
