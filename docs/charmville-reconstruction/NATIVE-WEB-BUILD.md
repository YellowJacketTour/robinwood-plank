# Local native web build checkpoint

The source runtime can now configure for WebAssembly on this Windows host. Installed isolated under sibling charmville-references/tooling: Emscripten6.0.6 (matching upstream configure script), CMake4.4.3, Ninja1.13.2, WinFlexBison2.5.25. No global PATH changes were required.

Run scripts/charmville/configure-native-web.ps1 from PowerShell. It resolves absolute tools/SDL paths, selects threaded SDL and configures zquest-classic/build_charmville_web. Baseline configuration succeeded after resolving missing parser tools and missing SDL paths. Emscripten built its sdl2-mt port successfully. Current build command: tooling/native-build-env/Scripts/cmake.exe --build zquest-classic/build_charmville_web --target zplayer --parallel 6 (resolve relative paths from references).

Configure log: main repo work/native-web-configure.log. Compiler log: reference checkout build_charmville_web/charmville-build.log. Existing web distribution and quest server are not replaced.

First compile stopped around step757/804 at src/play_midi.cpp: stock SDL_mixer lacks Mix_GetMusicBeat and Mix_SetMusicBeat. These are supplied by the upstream fork selected through web/patches/emscripten.patch. Next step is check/apply that patch to the dedicated SDK, fetch/rebuild the forked mixer, apply its matching patch and rebuild. Do not replace these calls with stubs: music timing is game functionality. PowerShell's trailing Get-Content returned zero despite Ninja failure; inspect the build log and preserve native exit status in future build wrappers.

Mixer follow-up: the full SDK patch has drifted against Emscripten6.0.6 and was not blindly applied. Adapted only tools/ports/sdl2_mixer.py to upstream's zc-fork-1 tag, exact SHA512 from web/patches/emscripten.patch and connorjclark/SDL_mixer URL. Added sdl2_mixer_mid and sdl2_mixer_mid_mt variants. The threaded MIDI port built successfully. Applied web/patches/sdl2_mixer.patch with git apply --ignore-space-change to the dedicated cache source (Windows whitespace mismatch); all hunks passed. Rebuild that patched port before final linking. General GME settings and other SDK/SDL/Allegro patches still need review. Do not claim the complete upstream patch set is installed.

When applying patches inside emsdk/upstream/emscripten, git resolves the emsdk repository root. Use git apply from that root with --directory=upstream/emscripten (and the appropriate cache subdirectory), and verify actual file changes; the original subdirectory invocation silently skipped paths.

Baseline zplayer linked successfully (zplayer.js and zplayer.wasm produced). Patched threaded mixer rebuilt, SHA256 CDCF9778F550D453BEDE39A78A55AA7F33B038B2B5FB34022A008D7F7E945D12. build-native-web.ps1 now sets SDK PATH for packaging and propagates target failure.

Packaging then reached missing generated timidity/soundfont-pats/oot.cfg and 2MGM.cfg. Upstream timidity/convert-sf.js requires the native unsf converter via UNSF environment variable; this conversion step has not run. Existing downloaded web-runtime/timidity also lacks these generated files. Do not fabricate empty configs or claim music complete. Generate patches from the checked-in soundfonts before retrying web_zplayer. Source bundle/npm installation completed; no browser distribution replaced.

Resolved: compiled psi29a/unsf in tooling/unsf/build-wasm with NODERAWFS, ENVIRONMENT=node, ALLOW_MEMORY_GROWTH and STACK_SIZE=5MB. The default small stack caused an out-of-bounds failure; 5MB converted both real soundfonts successfully, producing230 .pat files. Applied upstream config path rewriting. Repeat conversion with scripts/charmville/convert-native-soundfonts.mjs; the converter must already be built.

Latest web_zplayer packaging completed with exit0. Complete package is at reference checkout build_charmville_web/packages/web, including zplayer.wasm/js/data, play HTML, zscript and timidity assets. This has NOT been browser-smoke-tested or substituted for the working port3021 runtime. Actual zoom renderer changes remain pending. See main repo work/native-web-package.log for completion evidence.

Browser checkpoint superseding the untested status above: serve-reference-runtime.mjs --rebuilt now hosts this package on127.0.0.1:3024. Fixed the dataOrigin adapter to accept whitespace in debug bundles. Same browser tab18 reached rendered Autumn Town, timer advanced, HUD and Link visible; two directional key taps were sent. This is a startup/visual smoke test, not long movement, sound playback, camera zoom or multiplayer proof. User can return to3017 account game;3021 distribution remains intact. No new camera functionality yet.

This is baseline compilation, not camera zoom. Upstream web/patches/apply.sh contains required runtime patches and destructive reset/clean operations; do not execute it blindly against reusable checkouts. Apply/review patches to isolated SDK/dependency paths, rebuild changed libraries, then package the web_zplayer target. A successful compiler/linker result alone is not runtime acceptance. Camera changes require the renderer separation described in RENDERER-CUTOVER.md, followed by native/browser verification.

Timing correction 2026-09-13: visible game clock advanced 352 seconds during 181.171 seconds wall time (08:25:24.085 to08:28:25.256 UTC). The Allegro SDL heartbeat still invoked _al_timer_thread_handle_tick in addition to its normal timer. Reviewed/applied the complete upstream web/patches/allegro5.patch to the isolated dependency; its own comment identifies excessive framerate. Also includes upstream framebuffer-feedback, resize texture preservation and audio-buffer fixes. build-native-web.ps1 now checks the reverse patch before every build, applies a clean missing patch, and rejects partial/drifted state instead of silently shipping it. Timing rebuild log: work/native-web-timing-build.log. Browser timing acceptance pending; no player speed constants changed.

Browser timing acceptance: rebuilt web_zplayer exited0. Reloaded existing tab18 on3024, entered world. At08:31:46.377 UTC clock0:00:05; at08:32:26.776 UTC clock0:00:45. Forty game seconds over40.399 wall seconds, consistent with60Hz normal timing within whole-second HUD resolution. Earlier approximately1.94x speed is corrected. Same scene remains rendered. This does not certify camera zoom, sound quality, or full gameplay integration.
