# Active Charmville workstreams

## Integrated audit follow-up — 2026-09-12

Native sample history follow-up: native now retains64 actual frames in rotating files; observer replays missing contiguous records in sequence after short polling stalls. Overwritten/missing history falls back to latest observation and existing discontinuity validation, never invented path cells. Six bridge tests pass (including recorded corner replay and overwritten history), native quest compiled/published, and iframe revision changed to force reload. Client ordering audit passed23 focused cases with no speculative edits. Grounded long-walk visual acceptance remains open; airborne multi-cell landing remains unsupported. Ring filesystem cost is not yet measured.

Turn visibility correction: user reported walking now works but followers blink on turns. Removed the native presentation rule hiding a follower for overlap with the hero/other companions; tight trail bends naturally overlap actor footprints. Terrain/segment checks remain. This supersedes the earlier actor-ground-overlap rejection: visual followers are not solid blockers. Local quest compiled/published and retained tab reload requested; continuous turn motion still requires visual acceptance.

### Direction-change snap investigation

Source-mode audit follow-up: [NATIVE-WALK-CADENCE](NATIVE-WALK-CADENCE.md) distinguishes legacy, LTTP walking and newer hero movement. Do not assume 100ms is correct for the active quest until runtime flags are observed. Raw quest-byte interpretation did not establish those flags. The retained scene ran, but short input pulses did not establish sustained motion. The actor database regression now also verifies that presence expiry during a paced wait rejects the step and preserves the previous cell/sequence.

The next crop/social integration is specified in [HEART-IMPLEMENTATION](HEART-IMPLEMENTATION.md). Proposed balance values are not issued content; preserve the Oran grant and carry crop identity through renderer projections before introducing Heart seeds or gifts.

Negotiated cadence follow-up: actor snapshots now advertise `pacedMovement`; opted-in valid walking requests can wait at most200ms for their existing speed schedule while holding authority locks. Auth/session, active presence and home permission are rechecked after waiting. The same collision/sequence/speed validation executes again. Client dispatch pacing absorbs reply latency below100/142ms; legacy servers and travel preserve prior behavior. PostgreSQL schedule/replay checks passed;22 client cases passed together plus one added diagonal case passed separately; TypeScript and scoped lint passed. Socket gateway forwards to Next and requires no restart for this service change.

This eliminates an extra full post-ack wait in the negotiated path, not all throughput limits: RTT above a step interval and any native/server speed mismatch remain. Transaction-held waits consume connections and must be replaced with measured regional scheduling before scaling. Sustained native motion remains unverified.

Native position publishing previously ran every six frames and observer polling every100ms, which can miss adjacent8px cells at native walking speed. Native now publishes every settled frame; observer polls16ms with sequence deduplication. Rejected corrections preserve the current facing instead of replaying a historical turn; arrival corrections retain explicit facing. Four bridge tests and local quest compilation passed.

Movement recovery now recognizes an exact committed step after a lost POST acknowledgment without issuing a teleport/trail reset. Twenty movement-client tests passed, including reversed-direction/lost-ack and conflicting-authority cases. These are bounded fixes, not seamless movement acceptance.

Highest remaining movement priority: native speed can exceed the client's100ms-after-ack step throughput (especially with RTT), so sustained walking can still accumulate backlog and eventually correct. Resolve via a shared speed/input-time contract and authoritative batched observed inputs or native pacing; do not weaken collision/sequence validation or synthesize unobserved paths. Actual motion and delayed-network play remain required.

### Connected journey implementation

Latest continuation: resource receipts now invalidate the journal immediately, independent of whether inventory changes or its subsequent read succeeds. The journal selects the next cultivation lesson from saved receipts, distinguishes learned steps from current bed state, and renders unloaded milestones/inventory as unknown. TypeScript and scoped UI lint passed.

Follower arrival now uses ranks of active walkers, fits spacing to connected collision-tested trail length with a 16px minimum ground footprint, and bounds expansion toward the selected spacing. Segment checks and bend-overlap rejection prevent choosing blocked or overlapping ground positions. The candidate and local published quest compiled. Insufficient safe space still hides a follower; six-party motion, border travel and rendering cost remain unverified. This does not implement shared companion simulation.

`GET /api/charmville/journey` now derives six bounded accomplishments from authenticated historical receipts: explicit home claim, starter selection, own-home till/plant/water/positive-yield harvest. It grants nothing and does not infer progress from current inventory. `FirstSteps` consumes this endpoint, keeps balances separate, offers retry and returns players to the beds during cultivation. A ten-second bounded refresh remains; immediate receipt-triggered refresh and durable travel accomplishments are not yet connected.

An isolated PostgreSQL integration scenario passed for authentication, provenance, invalid/pending/cancelled/foreign actions, bought inventory exclusion, spending preservation and account isolation. The retained tab visibly showed the receipt-backed cultivation objective and Return to the beds action. This does not complete the authored first-hour journey or Burning Heart cultivation.

Native followers now discard scroll-frame trail coordinates and suppress walking/attack drawing during scrolling; accepted attack clocks/acknowledgments continue. Candidate and local published quest compilation succeeded. The six-member short-trail initialization limitation remains, and live border crossing is not yet visually accepted.

- Full scope and dependency map: [BUILD-PRIORITIES](BUILD-PRIORITIES.md). First-hour continuity, authoritative actions and custody precede catalog expansion.
- Movement queue now coalesces consecutive observations within the same logical cell. A delayed acknowledgment plus repeated stationary/subtile observations previously could evict real path cells and cause a false correction. Agent verification: 18 movement-client tests and scoped ESLint passed. Live border traversal remains unverified.
- Game video source and preview now stop on rendering loss, admission restart, canvas replacement, disposal and stale asynchronous playback. Agent verification: 12 capture/preview lifecycle tests passed. Root restarted the reference server and used the retained tab 15 to start and stop the local preview; the video element visibly displayed the running game canvas. This does not establish remote streaming, audio, latency or audience permissions.
- Root observed account identity text overlapped social buttons at the retained viewport. Navigation actions now occupy normal flow with 44px targets; a follow-up screenshot showed separate account and action rows. Other viewport widths remain unverified.
- Root ran `npx tsc --noEmit` successfully after these integrations. No release build, production deployment or full-game completion is claimed.
- Follower bridge recovery now keys its write cache by filesystem identity and quest directory and checks projection files still exist. Unchanged parties are rewritten after runtime file loss/replacement. Agent verification: three focused bridge tests and syntax passed. Native short trail initialization can still hide later party members, and scrolling trail/layer behavior still needs renderer work; this fix is not six-follower visual acceptance.
- Runtime server logs for this restart: `work/reference-runtime-video.log` and `work/reference-runtime-video-error.log`. The earlier log was locked, so the restart uses distinct files.

Evidence snapshot: 2026-09-12, local working tree. This document reconciles [verified progress](VERIFIED-PROGRESS.md), [next playable milestones](NEXT-PLAYABLE-MILESTONES.md) and [persistent world continuity](PERSISTENT-WORLD-CONTINUITY.md), with a small source audit. No new tests or browser interactions were run for this document. “Tested” and “visually verified” below mean specifically recorded checkpoint evidence, not acceptance of the entire workstream. No completion percentages are assigned.

Continue from `/charmville/world?panel=play` and the retained local pilot. Use synthetic local identities and simulated wallet presentation. Preserve saved custody; source catalogs and native guest counters are not owned account inventory.

| Workstream | Implemented scope | Tested evidence | Visually verified scope | Remaining / next acceptance gate |
|---|---|---|---|---|
| World | Account admission, native actor authority, paired d4/s62 ↔ d4/s63 border travel and persisted destination geometry; admission display gate; two-screen 64×22 atlas. Physical screens remain distinct from home/public account instances. | Continuity checkpoint records 19 actor/domain/client checks including isolated PostgreSQL reciprocal travel/reconnect, peer and placement regressions, atlas regression, targeted lint/TypeScript/native compilation. | Retained tab 15 showed the admission notice clear into the scene and the expanded atlas. Westward taps did **not** establish visible border traversal. | Record actual leave/return traversal and crop/companion placement through scrolling. Author distinct home/interior/public/friend-home geography, safe arrivals, continuous camera and broader topology. Expanded-screen crop/creature coordinates and dynamic collision parity remain open. |
| Combat / companions | Persisted bounded encounters, turn battle, capture, vitals/rest and individually owned followers; six-species diagnostic roster. Native hero combat remains local. Beam domain is explicitly unwired. | Focused companion, test-party, turn-battle, capture and lifecycle test files exist; this audit did not execute them or establish their current pass status. Source/service scope is described in verified progress. | Local Party panel and native followers are recorded; the pilot explicitly establishes no successful movement/combat playtest. Six simultaneous followers through turns, obstacles and travel are unverified. | One authoritative target identity/HP/status across world and staged views, complete directional action art, wall/interruption behavior, capture settlement and two-client reconnect. Verify all six followers; define competing-party reward/capture rights before contention. |
| Economy / crops | Server-authorized three-bed Oran lifecycle, crop identity migration 129, durable inventory and fixed-price exchange escrow/fill/cancel. Burning Heart is not a live crop. | [Native economic loop](NATIVE-ECONOMIC-LOOP.md) records harvest/reload and PostgreSQL escrow, revocation and duplicate-commit checks. [Live pilot](LIVE-PILOT-CHECKPOINT.md) records two resource and five client lifecycle/projection checks for identity. [Exchange](SHARED-WORLD-EXCHANGE.md) records lost-response retry, partial fill and cancellation with two synthetic accounts. | Oran receipt/inventory persistence is recorded; complete directional soil/tool contacts, interruption behavior and shared resource observation are not visually accepted. | Version Burning Heart entitlement/yield/renewal, author stages, add atomic sender debit/recipient credit and prove conservation/replay safety with two identities. Recheck Oran healing; then build a profession → craft → market chain. |
| UI / input | World shell, Party selection/detail/action UI, gamepad menu adapter and separate native equipment UI; tutorial canvas focus fix and same-region arrival retry. | Pilot records targeted UI lint and 15 movement-client tests for retry wiring, plus bridge syntax check. Historical [player checkpoint](PLAYER-EXPERIENCE-CHECKPOINT.md) records fullscreen, layout and synthetic-gamepad regressions; those are not current full-menu acceptance. | Pilot visibly entered public admission and completed introduction pages; clicking the native canvas opened its system menu before the focus fix. Updated runtime was reloaded. Instantaneous key pulses did not prove displacement. | One consistent Party/Equipment/Satchel/Map/Exchange menu language with focus, back, contextual feedback and error/loading states. Verify held keyboard input, physical controller and real touch independently in the retained playthrough. |
| Accounts / social | Guarded synthetic local account launcher, home access/presence, discovery catalogue and existing social/stamp services. Reviewed voice attachments exist in the separate PlankSpace composer. | [Voice attachments](VOICE-ATTACHMENTS.md) records three media tests and TypeScript. Pilot records local profile creation, home claim and starter selection. No complete social-gift or identity-linking acceptance is recorded. | Visible launcher/profile, home and starter interactions are recorded. Unified in-game feed/composition/gift journey and proximity voice are not accepted. | Resumable family/tutorial accomplishments, inventory-backed gift flow, friend-home roundtrip and integrated social UI. Stamp sender debit is not recipient inventory credit. Device identity linking, unified moderation and party/proximity voice remain separate work. |
| Performance | Local authenticated socket gateway targets 10 Hz snapshots and 5 Hz encounter projection, with HTTP fallback, bounded requests and a 32-connection cap; existing peer query cap is 16. It is not a region simulation worker. | [World socket](WORLD-SOCKET.md) documents two-client and native-socket verification commands; the existence of commands is not a current pass or capacity measurement. Local listeners were checked by the pilot. | Local scene/atlas rendering is recorded. No sustained multi-client load, external hosting, real-device latency or crash-recovery visual acceptance is established. | Measure p95/p99 simulation cost, bandwidth, delay/loss, overload, reconnect and worker failure. Implement region workers, spatial interest and durable resync before population claims or wider shared combat. |

## Current integration follow-up

The integrating agent reports a border-travel pacing fix in `lib/charmville/native-movement-client.ts`: travel now waits after the final walking acknowledgment. Its focused run passed 17 native movement-client tests, including the new final-step-to-travel delay regression. This documentation audit inspected that code and regression but did not rerun them. The same browser tab remains retained; visible border traversal is still unverified.

The integrating agent also observed localhost actor GET responses of 7–9 ms in a live log sample. This is a narrow request-latency observation, not end-to-end input latency, a percentile distribution, a load test or multiplayer capacity evidence.

## Reconciled discrepancies

- The opening requirement in `PERSISTENT-WORLD-CONTINUITY.md` still describes sole s63 support. Its later connected-screen section and `lib/charmville/native-atlas.ts` supersede that statement with two registered screens; they do not prove seamless camera or traversal visuals.
- `BURNING-HEART-CATALOG-CONTRACT.md` contains pre-migration crop-identity gaps. Migration 129 supplies Oran identity, not Burning Heart entitlement, goods, art or gifting.
- `SHARED-WORLD-EXCHANGE.md` opens with native movement/harvest disconnected from accounts. Newer actor and native-economic-loop evidence supersedes that integration boundary; its bounded exchange evidence remains useful.
- `PLAYER-EXPERIENCE-CHECKPOINT.md` describes temporary guest fertilizer/XP and says encounter/capture are unimplemented. Current bounded encounter/capture services supersede the latter; guest fertilizer/XP must not be counted as authenticated progression. Its “no new region” statement predates the two-screen work.
- Tests, visible play, final art and measured capacity are separate gates. Beam source, six-species fixtures, discovered emojis and runnable verification scripts cannot substitute for those gates.

## Connected delivery order

1. Close navigation/arrival and the common menu contract with a recorded first-hour journey.
2. Integrate Burning Heart custody and art into that journey, proving two-account gifting and reload conservation.
3. Close the shared creature expedition with one authority contract and verified six-follower travel.
4. Expand professions, creator packs and geography only with concrete acquisition/use/ownership and visual acceptance; validate worker/recovery capacity before larger populations.

Each update should name its changed paths, exact runtime/content revision, focused checks, visible outcome and remaining limits. A release still requires the repository release checks; this documentation audit is not a release certification.

## Live input follow-up

The retained live tab opened and closed Game menu, but focus remained visibly on the opener. The close handler now waits for dialog restoration, ensures canvas tabindex, and returns focus unless another dialog/application owns it. This change still needs a fresh runtime load and visible movement verification; do not count key taps as proof of gameplay.
